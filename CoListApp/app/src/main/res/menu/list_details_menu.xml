<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
	xmlns:app="http://schemas.android.com/apk/res-auto">
	<item android:id="@+id/shareWithUserItem"
		android:title="Share"
		android:icon="@drawable/ic_share_with_user_icon"
		android:iconTint="@color/white"
		app:showAsAction="always"
		/>

	<item android:id="@+id/filterItem"
		android:title="Filter"
		android:icon="@drawable/ic_filter_icon"
		android:iconTint="@color/white"
		app:showAsAction="always"
		/>

	<item android:id="@+id/moreOptionsItem"
		android:title="More"
		android:icon="@drawable/ic_more_options_icon"
		android:iconTint="@color/white"
		app:showAsAction="always"
		/>
</menu>